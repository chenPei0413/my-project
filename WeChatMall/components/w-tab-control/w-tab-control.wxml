<!--tab-control/tab-control.wxml-->
<view class="tab-control">
  <view wx:for="{{titles}}" wx:key="index" class="tab-item {{currentIndex == index ? 'active' : ''}}" catchtap="handleitemClick" data-index="{{index}}">
    <text>{{item}}</text>
  </view>
</view>